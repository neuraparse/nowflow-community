import { EnterpriseFeaturePage } from '../components/enterprise-feature-page'

export default function AgentProfilesPage() {
  return (
    <EnterpriseFeaturePage
      eyebrow="Enterprise"
      title="Agent"
      accent="Profiles"
      description="Reusable agent identities, shared skills, and workspace-level profile governance are available in Enterprise."
    />
  )
}
